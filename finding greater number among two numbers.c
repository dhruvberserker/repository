#include <stdio.h>
#include <conio.h>

void main()
{
    int a,b;
    printf("Enter two numbers");
    scanf("%d%d,&a,&b");
    if (a>b)
    printf("number is %d",a);
    else
    printf("number is %d",b);
    return 0;
}
